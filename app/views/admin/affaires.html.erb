<div class="container">
    <h1 class="text-center">Administrator Dashbord</h1>
    <%= render 'admin/nav_links'%>
</div>

    <table class="table table-dark table-striped mt-3"> 
        <thead>
            <th scope="col", class="text-center">Numéro</th>
            <th scope="col", class="text-center">Client</th>
            <th scope="col", class="text-center">title</th>
            <th scope="col", class="text-center">Heure Saisie</th>
        </thead>
    
        <tbody>
          <% @affaires.each do |affaire| %>
            <tr scope="row"> 
              <th class="text-center"><%= affaire.number.truncate(8)%></th>
              <td class="text-center"><%= affaire.client%></td>
              <td class="text-center"><%= affaire.title%></td>
              <td class="text-center"><%= link_to Time.at(affaire.workaffaire).utc.strftime("%HH"), admin_affaire_path(affaire) %></td>  
            </tr>
          <%end%>  
        </tbody>
    </table>

