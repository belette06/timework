<div class="container text-center">
  <p>
    <%= link_to "Admin", admin_path%> <%= " > #{params[:action].capitalize}" unless params[:action].eql?("index") %>
  </p>  
  <div class="btn-group">
    <%= render "admin/links", resource: User %>
    <%= render "admin/links", resource: Weektime %>
    <%= render "admin/links", resource: Affaire %>
    <%= render "admin/links", resource: Depannage %>
  </div>
</div>

