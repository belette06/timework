  <div>
    <% flash.each do |type, msg| %>
        <% if type == 'alert' %>
          <%= content_tag :div, msg, class: "alert alert-danger", role: :alert %>
          <% else %>
          <%= content_tag :div, msg, class: "alert alert-primary", role: :alert %>
        <% end %>
    <% end %>
  </div>

<%= simple_form_for [@weektime, @worktime] do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs text-center" >
      <%#= f.input :daytime,  wrapper: :horizontal_collection_inline, collection: Worktime.daytimes.keys  %>
      <h5> Selection le jour </h5>
      <%= f.input :daytime, as: :radio_buttons, collection:Worktime.daytimes.keys, label: false, wrapper: :horizontal_collection_inline %>      
    </div>
</br>
  <div class="form-inputs text-center">
    <h5> Selection les heures </h5>
    Debut <%= f.input :gotime, as: :time , label: false %>
    Fin <%= f.input :endtime, as: :time , label: false  %> 
  </div>

  <div>
    <h5> Selection laffaire </h5>
 <%= f.association :affaire, collection: Affaire.all, :value_method => :id %> 
  </div>

</br></br>
  <div class="form-actions text-center">
    <%= f.button :submit , class:"btn btn-outline-secondary btn-sm"  %>
  </div>
<% end %>
