 <%if @weektimes.empty? %> 
  <tr>
    <td colspan="4", class="text-center"><strong>Aucune de feuille d'heure ajouté</strong></td>
  </tr>
 <% else %>   
   <% @weektimes.each do |weektime| %>
   
     <tr scope="row">
      <td class="text-center"><%= weektime.numsemaine %> | <%= weektime.dateweek.strftime("%Y-%m") %> </td>
      <td class="text-center"><%= weektime.workweek %> Hrs </td>
      <td class="text-center"><%= weektime.accord %></td>
      <td class="text-center"> </td> 
      <td class="text-center">  <%= link_to "Voir", weektime, class:"btn btn-outline-info btn-sm"%>   
     <%= link_to "X", weektime_path(weektime.id), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } , class: "btn btn-outline-danger btn-sm" %> </td>
     </tr>

   <% end %>    
 <%end%>  
